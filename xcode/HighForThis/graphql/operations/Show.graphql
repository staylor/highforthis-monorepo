query Show($id: ObjID!) {
  show(id: $id) {
    artist {
      appleMusic {
        artwork {
          height
          url
          width
        }
        id
        url
      }
      id
      name
      slug
    }
    date
    id
    title
    venue {
      id
      name
      slug
    }
  }
}
