query Show($id: String!) {
  show(id: $id) {
    id
    date
    title
    artists {
      id
      name
      slug
      appleMusic {
        id
        url
        artwork {
          height
          url
          width
        }
      }
    }
    venue {
      id
      name
      slug
    }
  }
}
