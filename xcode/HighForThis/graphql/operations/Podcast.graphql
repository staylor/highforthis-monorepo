query Podcast($id: ObjID!) {
    podcast(id: $id) {
        audio {
            destination
            duration
            fileName
            id
        }
        description
        id
        title
    }
}
