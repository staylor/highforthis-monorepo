[build]
watchPatterns = ["graphql/**"]
installCommand = "corepack enable && pnpm install"
buildCommand = "pnpm build"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
