[build]
installCommand = "corepack enable && pnpm install"
buildCommand = "pnpm build && pnpm prod:upload"

[deploy]
startCommand = "pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
